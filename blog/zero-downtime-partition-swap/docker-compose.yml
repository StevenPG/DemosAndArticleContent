services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: eventsdb
      POSTGRES_USER: events
      POSTGRES_PASSWORD: events
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:

// Root project is an aggregator only; each module declares its own plugins and
// dependencies so the build files read as standalone examples in the blog post.
allprojects {
    group = "com.example"
    version = "0.0.1-SNAPSHOT"
}

# The single function this Lambda exposes. FunctionInvoker reads this to decide
# which function to route the invocation to. It is the SAME composed pipeline the
# web, kafka and rsocket surfaces use.
spring.cloud.function.definition=enrichOrder|validateOrder

spring.application.name=adapter-aws

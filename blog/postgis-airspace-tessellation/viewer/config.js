// Optional: paste a Cesium Ion access token (free at https://ion.cesium.com)
// to render Cesium World Terrain + Bing imagery under the airspaces.
//
// Without a token the viewer automatically falls back to the free, key-less
// ESRI World Elevation terrain + ESRI World Imagery — the mountain still
// renders, no signup required. (Only if neither is reachable does it drop to
// a flat ellipsoid with OpenStreetMap tiles.)
window.CESIUM_ION_TOKEN = "";

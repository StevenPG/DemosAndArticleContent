// Optional: paste your Cesium Ion access token (free at https://ion.cesium.com)
// to render real Cesium World Terrain + Bing imagery under the airspaces.
// Leave empty to fall back to a flat ellipsoid with OpenStreetMap imagery —
// the airspace volumes still render at their true ellipsoidal heights, they
// just won't have the mountain underneath them.
window.CESIUM_ION_TOKEN = "";

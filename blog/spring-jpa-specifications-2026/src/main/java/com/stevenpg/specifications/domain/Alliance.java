package com.stevenpg.specifications.domain;

/** Airline alliance membership. Mapped as an enum so we can show enum predicates. */
public enum Alliance {
    STAR_ALLIANCE,
    ONEWORLD,
    SKYTEAM,
    UNALIGNED
}

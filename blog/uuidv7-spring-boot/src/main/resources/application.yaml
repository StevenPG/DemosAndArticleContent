spring:
  application:
    name: uuidv7-spring-boot
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        jdbc:
          batch_size: 100
        order_inserts: true
  sql:
    init:
      mode: never

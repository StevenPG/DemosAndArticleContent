# Virtual threads — each incoming HTTP request lands on its own virtual thread
# instead of borrowing from a fixed platform-thread pool. Virtual threads are cheap
# (JDK manages millions), so blocking I/O calls like our echo server call don't
# waste OS threads waiting.
spring.threads.virtual.enabled=true

# Corpus

Drop markdown files in this directory and they'll be ingested into the vector
store on startup. For the article demo, copy the blog's posts here:

```bash
cp /path/to/blog/src/content/blog/*.md ./corpus/
```
